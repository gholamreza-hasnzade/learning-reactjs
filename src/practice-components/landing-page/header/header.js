import React from "react";

// * import style
import "./header.css";

const Header = () => {
    return (
        <section>
            <span>Header in website</span>
        </section>
    );
};

export default Header;
